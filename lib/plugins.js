"use strict";

var linz = require('../');

// used to register a plugin with linz
var register = function (name, path, init) {



};

module.exports = {

    // expose public functions
    register: register

};
