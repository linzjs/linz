'use strict';

const arrayRenderer = require('./array');
const booleanRenderer = require('./boolean');
const dateRenderer = require('./date');
const overviewLinkRenderer = require('./overview-link');
const referenceRenderer = require('./reference');

module.exports = {
    arrayRenderer,
    booleanRenderer,
    dateRenderer,
    overviewLinkRenderer,
    referenceRenderer,
};
