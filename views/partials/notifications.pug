script.
    var notifications = !{JSON.stringify(notifications)};

    $(document).ready(function () {

        for (var i = 0; i < notifications.length; i++) {
            new Noty(notifications[i]).show();
        }

    });
