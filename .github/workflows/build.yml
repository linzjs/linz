name: build
on:
    push:
        branches-ignore:
            - 'main'
        tags:
            - 'v*'
jobs:
    buildLinz:
        name: Build Linz
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v3
            - name: Testing linz
              run: yarn test-start
              shell: 'script -q -e -c "bash {0}"'
